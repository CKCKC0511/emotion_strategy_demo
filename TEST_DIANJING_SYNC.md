# 点睛 → 情感策略demo 同步测试文档

## 测试目标

验证在"情感策略点睛"子tab中完成点睛生成后，点击"同步设置到情感策略demo"按钮，以下信息能否正确替代情感策略demo中的默认信息。

## 同步项清单

| 序号 | 同步项 | 默认值 | 同步来源 | 影响位置 |
|------|--------|--------|----------|----------|
| 1 | 角色姓名 (char_name) | 恩佐 | 点睛输入的"角色姓名"字段 | 系统提示词第一行「{{char_name}}」 |
| 2 | 角色简介 (base_profile) | 恩佐的完整base_profile | 点睛输入的"角色简介"(tagline)字段 | 系统提示词中的「{{base_profile}}」 |
| 3 | 各状态行动指令 (perception) | 默认的director_note_prompt | 点睛结果的script（语气+动作描写方向） | 系统提示词中的「{{director_note_prompt}}」、角色设定tab |
| 4 | IDT区间 | 默认的state range | 点睛结果的stateTable | 角色设定tab + 状态路由判定 |
| 5 | 优先级 (priority) | 默认的priority band | 点睛结果的priorityTable | 角色设定tab + 状态路由判定 |

---

## 测试用例

### TC-01: 默认状态验证（未执行点睛前）

**步骤：**
1. 刷新页面，确保干净状态
2. 在"情感策略demo"对话区发送一条消息（如"你好"）
3. 在右侧"对话节点可视化"查看"最终系统提示词"

**预期结果：**
- 系统提示词第一行包含 `当前扮演角色「恩佐」`
- `base_profile` 部分包含 `恩佐，27岁，是一位拥有生物学与犯罪现场调查双学位的独立调查员`
- `director_note_prompt` 为默认的状态感知文案
- 状态路由基于默认IDT区间和优先级

**实际结果：** ✅ 通过
- 系统提示词第一行：`你是一位专业的小说角色扮演专家，当前扮演角色「恩佐」。`
- base_profile 包含恩佐完整设定
- 状态路由使用默认IDT区间

---

### TC-02: 点睛生成并同步

**步骤：**
1. 在"情感策略demo"右侧点击"情感策略点睛"子tab
2. 填写以下测试数据：
   - 角色姓名：`苏晗`
   - 角色性别：`女`
   - 核心设定与原始标签：`冷面学霸, 隐藏甜心`
   - 角色简介：`苏晗，22岁，顶尖大学物理系的天才研究生。表面冷淡寡言，实际内心细腻敏感。`
   - 原始开场白：`她抬起头看了你一眼，又低下头继续写公式。`
3. 点击"生成点睛结果"，等待结果返回
4. 确认点睛结果包含：剧本（script）、状态表（stateTable）、状态优先级（priorityTable）
5. 点击"同步设置到情感策略demo"按钮

**预期结果：**
- 页面自动切换到"情感策略demo" → "角色设定"子tab
- 角色设定中各状态的 director note 更新为点睛生成的语气+动作描写方向
- IDT区间更新为点睛生成的数值
- 优先级更新为点睛生成的优先级

**实际结果：** ✅ 通过
- 点睛成功生成4个状态的行动指令、IDT区间、优先级
- 示例生成结果：
  - 日常兜底：语气=声线平偏低，语速慢，话少精简，藏着不易察觉的软意
  - 试探推拉：语气=带刻意的淡疏离，话里藏钩子，音量轻却留有余味
  - 失控破防：语气=尖细压着慌，语速乱了节奏，咬唇憋住哽咽的哭腔
  - 极度羁绊：语气=软得发颤带鼻音，话碎且直白，全是卸下心防的剖白

---

### TC-03: 同步后对话验证 — char_name 替换

**前置条件：** TC-02 已执行，点睛结果已同步

**步骤：**
1. 切换到"对话节点可视化"子tab
2. 在对话区发送一条消息（如"你好呀"）
3. 查看右侧可视化面板的"最终系统提示词"

**预期结果：**
- 系统提示词第一行变为 `当前扮演角色「苏晗」`（不再是"恩佐"）

**实际结果：** ✅ 通过
- 系统提示词第一行：`你是一位专业的小说角色扮演专家，当前扮演角色「苏晗」。`

---

### TC-04: 同步后对话验证 — base_profile 替换

**前置条件：** TC-02 已执行，点睛结果已同步

**步骤：**
1. 在TC-03的基础上，继续查看"最终系统提示词"

**预期结果：**
- `base_profile` 部分变为 `苏晗，22岁，顶尖大学物理系的天才研究生。表面冷淡寡言，实际内心细腻敏感。`（不再是恩佐的profile）

**实际结果：** ✅ 通过
- base_profile 成功替换为苏晗的设定
- 不再包含恩佐的profile

---

### TC-05: 同步后对话验证 — director_note_prompt 替换

**前置条件：** TC-02 已执行

**步骤：**
1. 在TC-03基础上，查看"最终系统提示词"中的"情绪策略"部分
2. 同时在"角色设定"子tab检查各状态的 director note 值

**预期结果：**
- 系统提示词中的 `{{director_note_prompt}}` 填入的是点睛生成的当前状态对应的语气和动作描写方向（格式如：`语气：xxx；动作描写方向：xxx`）
- 而非默认的旧文案

**实际结果：** ✅ 通过
- 系统提示词中【情绪策略】字段内容为：`语气：声线平偏低，语速慢，话少精简，藏着不易察觉的软意；动作描写方向：指尖停在公式上，偶尔抬眼递温牛奶`

---

### TC-06: 同步后对话验证 — IDT区间和状态路由

**前置条件：** TC-02 已执行

**步骤：**
1. 在"角色设定"子tab查看4个状态的IDT区间
2. 发送多条对话，触发不同的IDT变化
3. 在"对话节点可视化"中查看 State Routing 结果

**预期结果：**
- 角色设定中的IDT区间数值来自点睛结果（与点睛的状态表一致）
- 状态路由判定使用新的IDT区间

**实际结果：** ✅ 通过
- IDT(15,80,30) 正确路由到"日常兜底"(D=60-100, T=30-70 匹配)
- 未使用 fallback 而是直接匹配成功

---

### TC-07: 同步后对话验证 — 优先级

**前置条件：** TC-02 已执行

**步骤：**
1. 在"角色设定"子tab检查各状态的优先级
2. 若有多个状态同时命中，查看哪个被选为当前状态

**预期结果：**
- 优先级值来自点睛结果（如 P1、P2、P3、P4）
- 多状态命中时按新的优先级排序

**实际结果：** ✅ 通过
- 日常兜底 primaryBand=P4, priority=4

---

### TC-08: 自动评测同步验证

**前置条件：** TC-02 已执行

**步骤：**
1. 切换到"评测"子tab
2. 点击"开始自动评测"

**预期结果：**
- 评测过程中使用的系统提示词包含新的 char_name（苏晗）和 base_profile
- 评测结果的 state_key 和 director_note_prompt 基于点睛同步的设定

**实际结果：** ✅ 通过
- 评测 API 正确接收 charName="苏晗" 和 baseProfile
- 状态路由使用同步后的 perception（语气：声线平偏低）
- AI回复内容明确扮演女性物理系研究生（苏晗角色）

---

### TC-09: 刷新页面后重置验证

**步骤：**
1. 在TC-02同步成功后，刷新页面（F5）
2. 发送一条对话消息
3. 查看"最终系统提示词"

**预期结果：**
- char_name 恢复为"恩佐"
- base_profile 恢复为恩佐的默认profile
- 各状态的 director note、IDT区间、优先级恢复为默认值
- （说明：所有同步结果仅在当前会话有效）

**实际结果：** ✅ 通过
- 系统提示词第一行恢复为：`你是一位专业的小说角色扮演专家，当前扮演角色「恩佐」。`
- base_profile 恢复为恩佐完整设定
- 会话级修改正确重置

---

## 测试执行记录

| 用例 | 状态 | 备注 |
|------|------|------|
| TC-01 | ✅ 通过 | 默认恩佐角色正常工作 |
| TC-02 | ✅ 通过 | 点睛生成4状态完整数据 |
| TC-03 | ✅ 通过 | char_name 成功替换为"苏晗" |
| TC-04 | ✅ 通过 | base_profile 成功替换 |
| TC-05 | ✅ 通过 | director_note_prompt 使用点睛生成的语气+动作描写 |
| TC-06 | ✅ 通过 | IDT区间正确更新，路由判定正确 |
| TC-07 | ✅ 通过 | 优先级正确更新 |
| TC-08 | ✅ 通过 | 自动评测使用同步后的角色设定 |
| TC-09 | ✅ 通过 | 刷新页面后所有设定恢复默认 |

## 结论

所有 9 项测试用例全部通过。点睛完成后同步到情感策略demo的 5 项数据（角色姓名、角色简介、行动指令、IDT区间、优先级）均能正确替代默认值，并在对话、状态路由、自动评测中生效。所有变更为会话级，刷新后正确重置。
