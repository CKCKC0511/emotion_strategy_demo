# 情感策略demo - 测试方式与自测记录

## 文档说明

本文档基于 `APP_FEATURES_AND_TECH_STACK.md` 编写，目标是给当前 app 提供一份可执行的测试方式说明，并记录一次本地自测结果。

适用场景：

- 开发完成后自查
- 交付前验收
- 回归测试
- 对外演示前快速确认系统是否正常

---

## 一、测试目标

当前测试目标分为 4 类：

1. 基础可用性
   - 服务能正常启动
   - 页面能加载
   - 核心接口返回正常

2. 核心业务链路
   - 情感策略对话链路
   - 丰荣链路
   - 点睛链路
   - 自动评测链路

3. 配置与会话级能力
   - 提示词修改
   - 状态配置修改
   - 点睛同步到情感策略 demo
   - 刷新后恢复默认

4. 可视化与展示
   - 对话节点可视化
   - 自动评测 Session
   - 丰荣 / 点睛结果展示

---

## 二、测试分层

建议把测试分成下面 3 层执行：

### 1. 接口 smoke test

目的：

- 确认服务存活
- 确认主接口没有直接报错
- 确认返回结构完整

适合场景：

- 本地开发后快速自查
- 部署后第一时间排障

### 2. 页面功能测试

目的：

- 确认 UI 展示和交互逻辑正常
- 确认异步流程不会阻塞主流程

适合场景：

- 提交前人工走查
- 演示前检查

### 3. 回归测试

目的：

- 确认改动没有破坏已有能力
- 尤其关注提示词、状态配置、自动评测、同步链路

适合场景：

- 新增需求后
- 修改状态逻辑后
- 修改前端 tab 结构后

---

## 三、测试环境

建议环境：

- OS: macOS
- Node.js: `>= 18`
- 本地服务端口：`3456`
- 大模型服务：`Volcengine Ark`
- 当前模型：`doubao-seed-2-0-lite-260215`

运行前提：

- 已配置 `ARK_API_KEY`
- 本地服务已启动：`node server.js`

建议先访问：

- `http://localhost:3456`
- `http://localhost:3456/api/health`

---

## 四、接口测试方式

## 1. 健康检查

### 测试目的

- 验证服务是否正常启动

### 测试方式

请求：

```bash
curl http://127.0.0.1:3456/api/health
```

### 预期结果

- 返回 `200`
- 返回字段：
  - `ok: true`
  - `model`
  - `hasArkApiKey`

---

## 2. 默认提示词接口

### 测试目的

- 验证系统默认提示词是否可读
- 验证丰荣/点睛提示词是否已纳入接口返回

### 测试方式

请求：

```bash
curl http://127.0.0.1:3456/api/prompt-defaults
```

### 预期结果

- 返回 `200`
- 返回字段包含：
  - `replyPrompt`
  - `scorePrompt`
  - `evaluationPrompt`
  - `userSimulationPrompt`
  - `fengrongPrompt`
  - `dianjingPrompt`

---

## 3. 情感策略聊天接口

### 测试目的

- 验证聊天主链路是否正常
- 验证节点 trace 是否返回完整
- 验证当前 4 状态配置是否生效

### 测试方式

发送一条最小聊天请求：

```json
{
  "messages": [
    { "role": "system", "content": "你是简洁、友好的中文助手。" },
    { "role": "user", "content": "你好，今天辛苦了。" }
  ],
  "currentIdt": { "i": 15, "d": 80, "t": 30 },
  "roleStateConfig": [/* 当前4状态配置 */]
}
```

### 预期结果

- 返回 `200`
- 包含：
  - `assistant.content`
  - `trace.scoring`
  - `trace.pad`
  - `trace.idt`
  - `trace.routes`
  - `trace.reply.systemPrompt`
  - `trace.stateMeta`

---

## 4. 丰荣接口

### 测试目的

- 验证丰荣接口可正常生成 `cha_set`

### 测试方式

输入角色基础信息调用：

```json
{
  "name": "Enzo",
  "gender": "male",
  "coreTags": "forensic investigator, occult cases, razor-sharp logic",
  "tagline": "A private investigator who dissects impossible cases with surgical calm.",
  "opener": "你敲门的时候，他刚合上一本旧笔记本。"
}
```

### 预期结果

- 返回 `200`
- 包含：
  - `charSet`
  - `trace.fengrong.latencyMs`

---

## 5. 点睛接口

### 测试目的

- 验证点睛接口能返回完整结构化结果

### 测试方式

传入一个 `charSet` 文本调用：

```json
{
  "charSet": "..."
}
```

### 预期结果

- 返回 `200`
- `dianjing.script` 包含 4 个状态
- `dianjing.stateTable` 长度为 4
- `dianjing.priorityTable` 长度为 4

---

## 6. 单轮评测接口

### 测试目的

- 验证自动评测的单轮执行能力

### 测试方式

请求：

```json
{
  "messages": [
    { "role": "system", "content": "你是简洁、友好的中文助手。" }
  ],
  "currentIdt": { "i": 15, "d": 80, "t": 30 },
  "round": 1,
  "roleStateConfig": [/* 当前4状态配置 */]
}
```

### 预期结果

- 返回 `200`
- 包含：
  - `detail`
  - `nextMessages`
  - `nextIdt`
- 如果模型正常，应当 `detail.error = null`

---

## 五、页面人工测试方式

## 1. 情感策略demo - 对话链路

### 步骤

1. 打开首页
2. 在对话区输入一条消息
3. 点击 `发送`

### 预期结果

- 用户消息立即显示
- 助手回复正常显示
- 右侧 `对话节点可视化` 正常渲染
- 不出现阻塞或长时间空白

---

## 2. 情感策略demo - 自动评测 Session

### 步骤

1. 发送一条消息
2. 观察对话区下方 `自动评测 Session`

### 预期结果

- 回复先返回
- 节点可视化先显示
- 自动评测 session 在后台异步推进
- session 会逐步更新：
  - 进度
  - 均分
  - 逐轮明细

### 关注点

- 自动评测不能阻塞主回复
- 多轮消息后应新增多个 session

---

## 3. 情感策略demo - 角色设定

### 步骤

1. 打开 `角色设定`
2. 修改某个状态的：
   - `director note`
   - `IDT 区间`
   - `优先级`
3. 发送新消息

### 预期结果

- 节点可视化里的 `State Routing` 规则展示应变化
- 路由命中结果应按新配置执行
- 回复系统提示词中的 `director_note_prompt` 应反映新配置

---

## 4. 丰荣页面

### 步骤

1. 切到 `丰荣`
2. 填写基础信息
3. 点击生成

### 预期结果

- 丰荣结果正常生成
- `cha_set` 文本可读
- 修改丰荣提示词后再次生成，结果会随之变化

---

## 5. 点睛页面

### 步骤

1. 切到 `点睛`
2. 填写基础信息
3. 点击生成

### 预期结果

- 页面会先准备 `cha_set`
- 最终输出统一的 `点睛结果`
- 页面内包含：
  - 剧本
  - 状态表
  - 状态优先级

---

## 6. 点睛同步到情感策略demo

### 步骤

1. 在 `点睛` 页面生成一组结果
2. 点击 `同步设置到情感策略demo`

### 预期结果

- 自动切到 `情感策略demo -> 角色设定`
- 对应状态的：
  - `director note`
  - `IDT 区间`
  - `优先级`
  被同步更新
- 之后发送消息，聊天和评测应使用新配置

---

## 7. 刷新恢复默认

### 步骤

1. 在任意页面修改提示词或状态配置
2. 刷新浏览器

### 预期结果

- 所有会话级修改恢复默认
- 不应持久保留上次修改

---

## 六、推荐回归测试清单

每次改动后，至少回归以下项目：

- `api/health`
- `api/prompt-defaults`
- 普通聊天
- 自动评测 session
- 丰荣生成
- 点睛生成
- 点睛同步到情感策略demo
- 刷新后恢复默认

如果改动涉及状态逻辑，额外回归：

- `State Routing`
- `角色设定`
- `最终系统提示词`

---

## 七、本次自测记录

## 自测时间

- 日期：`2026-04-13`
- 方式：本地服务 + 接口 smoke test

## 已执行的自测项

### 1. 服务健康检查

结果：通过

关键信息：

- `status = 200`
- `ok = true`
- 当前模型为 `doubao-seed-2-0-lite-260215`

### 2. 默认提示词接口

结果：通过

验证点：

- `replyPrompt` 存在
- `fengrongPrompt` 存在
- `dianjingPrompt` 存在

### 3. 聊天接口

结果：通过

验证点：

- `status = 200`
- 返回了 `assistant.content`
- `trace.stateMeta.length = 4`
- 路由命中结果正常
- 返回了 `trace.reply.systemPrompt`

### 4. 丰荣接口

结果：通过

验证点：

- `status = 200`
- 成功返回 `charSet`
- 返回了 `fengrong` 耗时信息

### 5. 点睛接口

结果：通过

验证点：

- `status = 200`
- `script` 返回 4 个状态
- `stateTable` 长度为 4
- `priorityTable` 长度为 4

### 6. 单轮评测接口

结果：通过

验证点：

- `status = 200`
- 返回了 `detail`
- 返回了 `nextMessages`
- 返回了 `nextIdt`
- `detail.error = null`

---

## 八、本次未自动化验证的内容

以下内容本次没有通过浏览器人工逐项点击确认，只提供了测试方式：

- 对话区与自动评测 session 的完整联动体验
- 丰荣 / 点睛页面的完整 UI 手动交互
- 点睛同步到情感策略demo后的页面表现
- 刷新浏览器后恢复默认的完整手工验证

说明：

- 这些内容更适合人工验收
- 当前已通过接口级 smoke test 验证其底层主链路基本可用

---

## 九、建议的测试执行顺序

建议按以下顺序执行：

1. 健康检查
2. 默认提示词接口
3. 丰荣接口
4. 点睛接口
5. 聊天接口
6. 单轮评测接口
7. 页面人工测试
8. 回归测试

这样可以先确认底层链路可用，再做页面与交互验收。

---

## 十、结论

基于本次本地自测结果：

- 当前核心接口链路可用
- 4 状态配置链路可用
- 丰荣 / 点睛主流程可用
- 自动评测底层接口可用

当前版本适合继续做：

- 人工交互验收
- 演示前页面走查
- 下一轮功能迭代前的基线版本保存

