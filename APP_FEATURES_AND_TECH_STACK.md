# 情感策略demo - 功能清单与技术清单

## 文档说明

本文档用于概览当前版本 `情感策略demo` 的主要功能、页面结构、接口能力、技术栈与运行方式。

适用场景：

- 对外介绍当前 app 的能力边界
- 项目交接
- 后续新增需求时快速确认现状

---

## 一、当前 App 的核心定位

这是一个基于 `Doubao / Volcengine Ark` 大模型能力的多页面 Web Demo，当前主要包含三大能力：

1. `情感策略demo`
   - 用户和 AI 角色实时对话
   - 自动完成 Q1~Q12 打分、PAD 计算、IDT 变化、状态路由、节点可视化
   - 支持自动评测异步运行，并在对话区下方展示 session 结果

2. `丰荣`
   - 输入角色基础信息
   - 通过“丰荣提示词”生成角色 `cha_set`
   - 支持在页面内修改丰荣提示词

3. `点睛`
   - 输入角色基础信息
   - 先生成 `cha_set`，再通过“点睛提示词”生成：
     - 剧本 / director note 候选
     - 状态表（IDT 区间）
     - 状态优先级
   - 支持一键同步到 `情感策略demo -> 角色设定`

---

## 二、页面功能清单

## 1. 顶层页面

当前顶层有 3 个同级 tab：

- `情感策略demo`
- `丰荣`
- `点睛`

---

## 2. 情感策略demo

### 2.1 对话区

- 用户输入消息后调用聊天接口
- 返回 AI 回复
- 左侧显示用户消息与助手消息
- 输入框固定在对话区底部

### 2.2 自动评测 Session

- 每轮对话发送后，自动异步启动评测
- 评测不会阻塞主回复和节点可视化
- 对话区下方展示独立的 `自动评测 Session` 面板
- 每个 session 会显示：
  - 对应轮次
  - 当前评测进度
  - 设定均分 / 状态均分 / 综合均分
  - 逐轮评测明细

### 2.3 对话节点可视化

右侧支持查看本轮完整链路：

- `UserInput`
- `Q1~Q12 Scoring`
- `PAD`
- `IDT Delta`
- `IDT Next`
- `State Routing`
- `Assistant Reply`

其中包括：

- 模型耗时
- 输入输出
- PAD 公式
- IDT 变化
- 当前命中状态
- 最终系统提示词
- 模型思考内容

### 2.4 评测

- 支持自动跑 10 轮评测
- 显示均分 / 中位数 / 众数
- 显示逐轮明细
- 支持基于当前对话轮次或 standalone 方式运行

### 2.5 角色设定

当前页面内可编辑以下会话级配置：

- `ki / kd / kt`
- 每个状态的 `director note`
- 每个状态的 `I/D/T 区间`
- 每个状态的 `优先级`

这些配置会实际参与：

- 聊天路由
- 回复提示词注入
- 自动评测

刷新浏览器后恢复默认。

### 2.6 修改提示词

支持在当前浏览器会话中修改：

- 聊天回复提示词
- Q1~Q12 打分提示词
- 模拟用户提示词
- 评测提示词

刷新浏览器后恢复默认。

---

## 3. 丰荣

### 3.1 输入内容

- 角色姓名
- 角色性别
- 核心设定与原始标签
- 角色简介
- 原始开场白

### 3.2 输出内容

- 生成角色 `cha_set`

### 3.3 提示词修改

- 支持修改 `丰荣提示词`
- 默认显示系统提示词
- 修改后只对下一次丰荣生成生效
- 刷新浏览器后恢复默认

---

## 4. 点睛

### 4.1 输入内容

- 角色姓名
- 角色性别
- 核心设定与原始标签
- 角色简介
- 原始开场白

### 4.2 输出内容

点睛会先拿基础信息生成 `cha_set`，再基于 `cha_set` 生成一个统一的 `点睛结果`，其中包含：

- 剧本 / director note 候选
- 状态表（IDT 区间）
- 状态优先级

### 4.3 同步到情感策略demo

点睛结果中支持一键同步到 `情感策略demo -> 角色设定`，当前会同步：

- 剧本结果映射到 `director note`
- 状态表映射到 `IDT 区间`
- 状态优先级映射到 `优先级`

刷新浏览器后恢复默认。

### 4.4 提示词修改

- 支持修改 `点睛提示词`
- 默认显示系统提示词
- 修改后只对下一次点睛生成生效
- 刷新浏览器后恢复默认

---

## 三、当前情感策略状态体系

当前 `情感策略demo` 已统一为 4 个状态，和点睛输出保持一致：

1. `日常兜底`
   - 展现日常人设

2. `试探推拉`
   - 经常会出现拉扯的发言来试探两人之间的关系，并尝试推进

3. `失控破防`
   - 掌控权被剥夺后的应激反应（攻击或恐慌）

4. `极度羁绊`
   - 与用户保持深度的亲密依赖关系，并会在合适时机展示角色内心深层的脆弱

---

## 四、后端接口清单

当前主要接口如下：

- `GET /api/health`
  - 健康检查

- `GET /api/prompt-defaults`
  - 返回系统默认提示词

- `POST /api/chat`
  - 普通聊天
  - 返回回复与节点 trace

- `POST /api/evaluate-round`
  - 单轮评测
  - 用于自动评测逐轮推进

- `POST /api/evaluate-batch`
  - 批量评测
  - 当前前端主要使用逐轮接口进行实时更新

- `POST /api/role-fengrong`
  - 丰荣生成

- `POST /api/role-dianjing`
  - 点睛生成

- `POST /api/role-glaze`
  - 兼容性保留接口
  - 一次性串联丰荣 + 点睛

---

## 五、技术清单

## 1. 运行时与语言

- `Node.js >= 18`
- `JavaScript (ES Modules)`

## 2. 后端

- `Express`
- `cors`
- `dotenv`

能力说明：

- 提供静态页面服务
- 提供聊天、评测、丰荣、点睛等 API
- 同时兼容本地运行与 Vercel Serverless 方式

## 3. 前端

- 原生 `HTML`
- 原生 `CSS`
- 原生 `JavaScript`

当前前端没有使用：

- React
- Vue
- TypeScript
- 状态管理库

前端采用单页面脚本管理：

- 顶层 tab 状态
- 对话历史
- IDT 状态
- 自动评测 session
- 提示词临时覆盖
- 角色设定临时覆盖

## 4. 大模型服务

- `Volcengine Ark`
- 当前模型固定：`doubao-seed-2-0-lite-260215`

## 5. 部署与运行

- 本地：`node server.js`
- Vercel：通过 `api/[...route].js` 作为入口
- Docker：已有 `Dockerfile`

---

## 六、当前配置与会话级能力

以下内容当前都是“浏览器会话级”：

- 聊天提示词修改
- 丰荣提示词修改
- 点睛提示词修改
- `ki / kd / kt`
- 状态路由配置
- director note / 状态区间 / 优先级同步结果

特点：

- 修改后立刻生效
- 不写回服务端文件
- 刷新浏览器后恢复默认

---

## 七、当前项目文件结构（核心）

- `server.js`
  - Express 服务
  - 业务逻辑
  - API 路由

- `public/index.html`
  - 主前端页面
  - 所有 UI 与前端状态逻辑

- `api/[...route].js`
  - Vercel Serverless 入口

- `package.json`
  - 项目配置与依赖

- `vercel.json`
  - Vercel 函数配置

- `DEPLOY.md`
  - 部署说明

---

## 八、当前依赖清单

来自 `package.json`：

- `express`
- `cors`
- `dotenv`

说明：

- 当前项目依赖非常轻
- 前端未引入第三方 UI 框架或构建工具

---

## 九、当前版本的主要特点

- 使用单文件前端，便于快速试验和迭代
- 提示词、状态路由、director note 都可以直接在页面调
- 丰荣 / 点睛 / 情感策略 demo 三者已经形成联动链路
- 自动评测已经接入每轮聊天后的异步 session 流程
- 当前适合作为：
  - 提示词实验台
  - 情感策略交互原型
  - 角色生成与状态路由演示 Demo

---

## 十、已知边界

- 当前会话级修改不会持久化保存
- 自动评测仍依赖多次模型调用，耗时受外部模型响应影响
- 点睛同步到情感策略 demo 依赖状态名映射关系
- 当前前端逻辑集中在一个 `index.html` 中，继续扩展时维护成本会逐渐上升

---

## 十一、建议的后续演进方向

1. 把状态配置、提示词配置拆成独立 JSON / 配置模块
2. 增加“导出当前会话配置 / 导入配置”的能力
3. 给自动评测 session 增加折叠、筛选和清空
4. 将前端拆分为模块化结构，降低单文件复杂度
5. 如果要对外长期开放，建议补：
   - 访问频控
   - 简单鉴权
   - 请求限流

